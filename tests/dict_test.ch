# Simple dictionary creation and access
data = {"name": "Alice", "age": "30"}
name = data["name"]
age = data["age"]

# Empty dictionary
empty = {}

# Dictionary with integer keys
numbers = {1: "one", 2: "two", 3: "three"}
one = numbers[1]
