print("\n── Test: Working with large lists ──")

# Approach 1: Using a single list comprehension
print("\nApproach 1: Using a single list comprehension")
big_list1 = [i for i in range(0, 100)]
print("Length:", len(big_list1))
print("First 3:", big_list1[:3])

print("\n── All list tests done ──")
